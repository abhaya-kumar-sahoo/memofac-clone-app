import React, { Component } from 'react';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  dateviewContainer: {
    position: 'absolute',
    alignItems: 'center',
    // backgroundColor: '#44994455',
    width: 120,
    height: 150,
  },
});
